# CLI Chat Tool

1. **Start the Backend**
   Open a terminal and run the FastAPI server:
   ```bash
   uvicorn app.main:app --reload
   ```

2. **Run the CLI**
   Open a new terminal window inside the project root and run:
   ```bash
   python -m cli_tool.main
   ```